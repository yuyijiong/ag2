---
title: Prek
---

Run `prek install` to install prek into your git hooks. Before you commit, run
`prek run` to check if you meet the prek requirements. If you use Windows (without WSL) and can't commit after installing prek, you can run `prek uninstall` to uninstall the hook. In WSL or Linux this is supposed to work.
